node_modules/
npm-debug.log*
.DS_Store

# Secrets. .env holds the real database password and must never be committed;
# .env.example is the committed copy with placeholder values.
.env
.env.*
!.env.example

# Generated by the SQLite version of this project (see the a2-sqlite tag).
tasks.db
tasks.db-shm
tasks.db-wal
